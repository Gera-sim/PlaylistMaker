<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/trackView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"

    android:background="@color/search_error_button"
    android:orientation="horizontal"
    android:theme="@style/Theme.PlaylistMaker">

    <androidx.cardview.widget.CardView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/margin_8"
        app:cardCornerRadius="@dimen/corner_radius_2"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/iv_cover_playlist"
            android:layout_width="@dimen/pic_size_45"
            android:layout_height="@dimen/pic_size_45"
            android:background="@color/search_background"
            android:scaleType="centerCrop"
            android:src="@drawable/track_pic_312" />

    </androidx.cardview.widget.CardView>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_marginEnd="44dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/tv_playlist_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"

            android:ellipsize="end"
            android:fontFamily="@font/ys_display_regular"
            android:maxLines="1"
            android:textColor="@color/search_title"
            android:textSize="@dimen/text_16sp"

            tools:text="@string/playlist_name" />

        <TextView
            android:id="@+id/tv_playlist_tracks_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/margin_1"
            android:fontFamily="@font/ys_display_regular"
            android:maxLines="1"
            android:text="@string/playlist_track_counter"
            android:textColor="@color/grey"
            android:textSize="@dimen/text_11sp"
             />

    </LinearLayout>

</LinearLayout>